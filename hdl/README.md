# Verilog RTL

HDL sources live at [`artifacts/cpu-explorer/hdl/`](../artifacts/cpu-explorer/hdl/) (inside the Vite app so they bundle for the deployed `/verilog` page).

```bash
cd artifacts/cpu-explorer/hdl/riscv-pipeline-15stage
make sim
```
